<%= form_for([@post, @post.comments.build]) do |f| %>
	<%= f.label :name, '姓名' %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :body, '內容' %><br>
	<%= f.text_area :body %><br>
	<br>
	<%= f.submit '送出' %>
<% end %>